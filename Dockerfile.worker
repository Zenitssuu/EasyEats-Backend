# Use Node.js official image
FROM node:22

# Create working directory
WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy the whole project (your worker and services too)
COPY . .

# Run the worker
CMD ["npm", "run", "worker"]
